# Copy to /etc/systemd/system/helix-ui.service
# Edit User, WorkingDirectory, and ExecStart paths to match your setup
[Unit]
Description=H E L I X UI
After=network.target

[Service]
Type=simple
# CHANGE THESE to match your user and install path:
User=YOURUSER
WorkingDirectory=/home/YOURUSER/helix-ui
ExecStart=/home/YOURUSER/helix-ui/venv/bin/uvicorn app:app --port 7777 --host 0.0.0.0
Restart=always
RestartSec=3
EnvironmentFile=-/home/YOURUSER/helix-ui/.env

[Install]
WantedBy=multi-user.target
